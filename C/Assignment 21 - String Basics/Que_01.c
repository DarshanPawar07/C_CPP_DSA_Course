           
           /* Que - Write a program to calculate the length of the string.
                     (without using builtin method) */
           
           #include<stdio.h>
            int main()

                     {
                        char A[20];
                         int i;

                         printf("Enter Your Name\n");
                         scanf("%s",A);

                         for(i=0;i<=A[i];i++);
                         printf("Length Of The %s Is = %d\n",A,i);
      
                         return 0;

                      }
                     
                    