
     /* Que - Define a function to check whether a given number is a term in a Fibonacci series or not.  */


     #include<iostream>
     using namespace std;

    
           /*   bool isInFib(int num)
                                     {
                                          int a=-1,b=1,c=0;

                                          while(c<num)
                                                       {
                                                             c=a+b;
                                                             if(num==c)
                                                             return true;
                                                             a=b;
                                                             b=c;
                                                        }

                                          return false;
                                      }

                                      */ 